datacenter = "dc1"
data_dir   = "/opt/consul"
log_level  = "INFO"
server     = true
bootstrap_expect = {{ cluster_size }}
bind_addr  = "{{ hostvars[inventory_hostname].ansible_host }}"
client_addr = "0.0.0.0"

ui_config {
  enabled = true
}

ports {
  dns = 8600
  http = 8500
}